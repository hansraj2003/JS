console.log( null > 0);
console.log( null == 0);
console.log( null >= 0);

// Comparision operators converts null to number
// whereas equality check == does not.

console.log( undefined > 0);
console.log( undefined == 0);
console.log( undefined >= 0);

// === this is a type of equality check where it checks the datatypes too.

console.log( "2" === 2);